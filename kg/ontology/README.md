# Ontology

OWL/Turtle expression of the Domain Explorer registry. Use it to back the
knowledge graph (Neo4j or RDF triplestore) so reasoning can run on top of the
same shape Zod and Pydantic validate.

`domain_explorer.ttl` is a starter sketch — extend per phase as the registry grows.

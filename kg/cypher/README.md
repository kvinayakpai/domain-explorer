# openCypher templates

Reusable parametrized queries for the knowledge graph. Each file is a
single statement with `$param` placeholders so they can be reused across
Neo4j, Memgraph, or other openCypher-compatible engines.

The KG isn't wired yet — these templates are the contract for that phase.

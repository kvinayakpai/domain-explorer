# Node
node_modules/
.pnpm-store/
.next/
out/
dist/
build/
*.tsbuildinfo
.turbo/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env
.env.local
.env.*.local
.python-version
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.uv/

# dbt
target/
dbt_packages/
logs/
.user.yml

# DuckDB databases — regenerate locally via `python synthetic-data/generate_all.py --seed 42`
# (also too large for GitHub's recommended 50MB limit)
*.duckdb
*.duckdb.wal

# Synthetic-data CSV/parquet outputs — regenerated deterministically at seed=42
# Run `pwsh ./demo-prep.ps1` (or `python synthetic-data/generate_all.py --seed 42`) to recreate.
synthetic-data/output/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Coverage / test
coverage/
.coverage
htmlcov/
pytest-cache-files-*/

# Build artefacts and runtime detritus
.next/
**/.next/
node_modules/
**/node_modules/
domain-explorer.duckdb.wal
.cache/
.pytest_cache/
.ruff_cache/
__pycache__/
**/__pycache__/

# Local Python venvs
.venv/
_tmp_*

# KG binary store (regenerate via `npm run kg:build`); JSON snapshot is committed.
kg/graph.gpickle

# Sandbox-only test detritus.
_test_*.txt
_sync_probe.txt
_sync_check.txt

# Build artefact — produced by package-zip.ps1, not source.
domain-explorer-portable.zip

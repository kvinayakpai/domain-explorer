name: domain_explorer
version: 1.0.0
config-version: 2

profile: domain_explorer

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  domain_explorer:
    staging:
      +materialized: view
    marts:
      +materialized: table
